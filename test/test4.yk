class Scone {
    
    topping(first, second, third)
    {
        var str = third + " scones with " + first + "and " + second;
        pout str;
        rm(str);
    }
    
}
    
var scone = Scone();
scone.topping(String("strawberries "), String("cream"), 69); 


    

