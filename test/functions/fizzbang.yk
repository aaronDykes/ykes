
class Fizzbang
{

    fb()
    {
        var str;

        for (var i = 1; i < 100; i++)
            if ((i % 15) == 0)
            {
                str = "Fizzbang: " + String(i);
                pout(str);
                rm(str);
            }
            elif ((i % 5) == 0)
            {
                str = "Fizzbang: " + String(i);
                pout(str);
                rm(str);
            }
            elif ((i % 3) == 0)
            {
                str = "Fizz: " + String(i);
                pout(str);
                rm(str);
            }
            else 
                pout(i);

    }
}