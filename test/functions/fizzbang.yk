


class String {}

var str = String();

class Fizzbang
{

    res()
    {

        for (var i = 1; i < 10000; i++)
            if ((i % 15) == 0)
            {
                str.fb = "Fizzbang: " + (num -> string i);
                pout(str.fb);
            }
            elif ((i % 5) == 0)
            {
                str.bang = "Bang: " + (num -> string i);
                pout(str.bang);
            }
            elif ((i % 3) == 0)
            {
                str.fizz = "Fizz: " + (num -> string i);
                pout(str.fizz);
            }
            else 
                pout(i);




        pout(`${str.fb}, ${str.bang}, ${str.fizz}`);
        rm(str);

    }
}