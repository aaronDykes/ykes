


class String {}

class Fizzbang
{

    res()
    {
        var str = String();

        for (var i = 1; i < 100; i++)
            if ((i % 15) == 0)
            {
                str.fb = "Fizzbang: " + (num -> string i);
                pout(str);
            }
            elif ((i % 5) == 0)
            {
                str.bang = "Bang: " + (num -> string i);
                pout(str);
            }
            elif ((i % 3) == 0)
            {
                str.fizz = "Fizz: " + (num -> string i);
                pout(str);
            }
            else 
                pout(i);

        rm(str);

    }
}