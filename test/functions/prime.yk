function is_prime(n) {

    for (var i = 2; i < n; i++)
        if (n % i == 0)
            return false;

    return true;

}



function print_prime(n) {
    for (var i = 3; i < n; i++)
        if (is_prime(i))
            print i;

}

print_prime(10000);