sr p()
{
    sr is_prime(l)
    {
        if (l == 9) 
            return false;
        for (var i = 2; i < square(l); i++)
            if (l % i == 0)
                return false;

        return true;

    }

    sr print_prime(n)
    {
        for (var i = 3; i < n; i++)
        {
            if (is_prime(i))
                pout(i);
        }
    }

    print_prime(1000000);
}

var start = clock();
p();
pout(clock() - start);

    
