
sr ternary()
{

    var hello = (420 == 69) ? "nope" : "that works";

    // pout(
    //     hello,
    //     (69 == 69) ? "hello world" : "okay then"
    // );

    return hello;

}

sr null_coalescing()
{
    var b = ternary();
    var a = String("Hello world!");

    var hello = b ?? a;

    pout(hello);
}

// ternary();
null_coalescing();