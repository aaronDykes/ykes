sr sum(num1, num2)
{
    return num1 + num2;
}

sr arrays()
{
    var ar = ["Hello", "World", "Vector", "of", "Vectors"];

    for (var i = 0; i < ar.len; i++)
        pout ar[i];


    rm(ar); 
}

arrays();